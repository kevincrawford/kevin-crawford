import { useRef } from 'react'
const CaseSrudyMenu = (menu) => {
  return <div>Enter</div>
}

const data = {
  wheels: [
    {
      step: 1,
      title: 'Overview',
    },
  ],
}

export default CaseSrudyMenu
